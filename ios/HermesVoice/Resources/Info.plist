<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
	<key>CFBundleDisplayName</key>
	<string>Hermes Voice</string>
	<key>NSMicrophoneUsageDescription</key>
	<string>Hermes Voice listens for your voice to hold a live conversation and relay tasks to Hermes.</string>
	<key>UILaunchScreen</key>
	<dict/>
	<key>UIApplicationSceneManifest</key>
	<dict>
		<key>UIApplicationSupportsMultipleScenes</key>
		<false/>
	</dict>
	<!-- Populated per-configuration from Config/*.xcconfig via project.yml.
	     BridgeBaseURL is a plain URL, not a secret. There is deliberately
	     no bearer token / bootstrap credential key here: the client
	     session token that authenticates every protected bridge call is
	     minted at runtime by POST /v1/session and held only in Keychain
	     via KeychainSessionStore, never in Info.plist/xcconfig — see
	     docs/SECURITY.md. -->
	<key>BridgeBaseURL</key>
	<string>$(BRIDGE_BASE_URL)</string>
</dict>
</plist>
